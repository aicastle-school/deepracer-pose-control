# deepracer-pose-control

## Install

- Python version: `3.12`
- venv
    ```bash
    python -m venv venv
    source venv/Scripts/activate
    ```

- install
    ```bash
    pip install -r requirements.txt
    ```